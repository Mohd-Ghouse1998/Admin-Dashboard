/**
 * @deprecated - This file is kept for backward compatibility.
 * Please import from '@/modules/promotions' instead.
 */

import { promotionService } from '@/modules/promotions/services/promotionService';

// Re-export the main promotionService for backward compatibility
export { promotionService };
export default promotionService;
